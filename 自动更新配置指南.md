# 自动更新配置指南 - GitHub Releases

> 本文档说明如何配置基于 GitHub Releases 的自动更新功能

## 📋 目录

- [前提条件](#前提条件)
- [第一步：生成签名密钥](#第一步生成签名密钥)
- [第二步：配置 Tauri](#第二步配置-tauri)
- [第三步：发布新版本流程](#第三步发布新版本流程)
- [第四步：测试自动更新](#第四步测试自动更新)
- [常见问题](#常见问题)

---

## 前提条件

1. ✅ 已完成自动更新功能代码集成（已完成）
2. ✅ 有 GitHub 账号和仓库
3. ⚠️ 需要在 macOS 上安装 Tauri CLI

---

## 第一步：生成签名密钥

### 在 macOS 上操作

1. **安装 Tauri CLI**（如果还没安装）
```bash
npm install -g @tauri-apps/cli
```

2. **生成密钥对**
```bash
# 进入项目的 src-tauri 目录
cd /path/to/baimo_desktop/src-tauri

# 生成密钥（会保存到 ~/.tauri/baimo-desktop.key）
tauri signer generate -w ~/.tauri/baimo-desktop.key
```

3. **查看公钥**
```bash
# 运行上面的命令后，会显示类似这样的输出：
#
# Your keypair was generated successfully!
#
# Public key:
# dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IEFCQ0RFRkdISUpL...
#
# Private key: /Users/你的用户名/.tauri/baimo-desktop.key
#
# 复制 "Public key:" 下面的那一长串字符串，这就是公钥
```

4. **保存私钥**
⚠️ **重要**：私钥文件 `~/.tauri/baimo-desktop.key` 非常重要！
- 永远不要提交到 Git
- 妥善备份（建议存到安全的密码管理器）
- 每次发布更新都需要用它签名

---

## 第二步：配置 Tauri

### 修改 `src-tauri/tauri.conf.json`

找到 `plugins.updater` 部分，修改为：

```json
{
  "plugins": {
    "updater": {
      "active": true,
      "endpoints": [
        "https://github.com/你的GitHub用户名/你的仓库名/releases/latest/download/latest.json"
      ],
      "dialog": true,
      "pubkey": "这里粘贴第一步生成的公钥（一长串字符）"
    }
  }
}
```

**示例**（假设用户名是 `zhangsan`，仓库名是 `baimo-desktop`）：
```json
{
  "plugins": {
    "updater": {
      "active": true,
      "endpoints": [
        "https://github.com/zhangsan/baimo-desktop/releases/latest/download/latest.json"
      ],
      "dialog": true,
      "pubkey": "dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IEFCQ0RFRkdISUpL..."
    }
  }
}
```

### 提交配置文件

```bash
git add src-tauri/tauri.conf.json
git commit -m "配置自动更新：添加公钥和 GitHub Releases 端点"
git push
```

---

## 第三步：发布新版本流程

### 3.1 准备新版本

1. **更新版本号**

修改以下文件中的版本号（例如从 2.2.0 升级到 2.3.0）：

- `src-tauri/tauri.conf.json` → `version: "2.3.0"`
- `src-tauri/Cargo.toml` → `version = "2.3.0"`
- `package.json` → `version: "2.3.0"`
- `frontend/package.json` → `version: "2.3.0"`
- `frontend/src/components/settings/UpdateChecker.tsx` → `currentVersion = "2.3.0"`

2. **更新 CHANGELOG.md**（建议）

记录本次更新的内容，例如：
```markdown
## [2.3.0] - 2026-01-18

### 新增
- 自动更新功能：用户可从设置页面一键更新

### 改进
- 其他改进...
```

3. **提交版本更新**
```bash
git add .
git commit -m "chore: 发布版本 2.3.0"
git push
```

### 3.2 打包应用

**在 Windows 上打包**（因为目标是 Windows 应用）：
```bash
tauri-build-now.bat
```

打包完成后，安装包位置：
```
src-tauri\target\release\bundle\nsis\PNG部落记账_2.3.0_x64-setup.exe
```

### 3.3 对安装包签名

**在 macOS 上操作**：

```bash
# 进入项目目录
cd /path/to/baimo_desktop

# 使用 Tauri CLI 签名
tauri signer sign \
  "src-tauri/target/release/bundle/nsis/PNG部落记账_2.3.0_x64-setup.exe" \
  -k ~/.tauri/baimo-desktop.key
```

签名完成后，会显示类似：
```
Signature: dW50cnVzdGVkIGNvbW1lbnQ6IHNpZ25hdHVyZSBmcm9tIHRhdXJpIHNlY3JldCBrZXkK...
```

**复制这个签名字符串**，稍后需要用到。

### 3.4 创建 latest.json 清单文件

创建一个 `latest.json` 文件（可以在项目根目录临时创建）：

```json
{
  "version": "2.3.0",
  "date": "2026-01-18",
  "platforms": {
    "windows-x86_64": {
      "signature": "这里粘贴上面生成的签名字符串",
      "url": "https://github.com/你的用户名/你的仓库名/releases/download/v2.3.0/PNG部落记账_2.3.0_x64-setup.exe"
    }
  },
  "body": "## 更新内容\n\n### 新增\n- 自动更新功能：用户可从设置页面一键更新\n\n### 改进\n- 优化了性能和用户体验"
}
```

**注意**：
- `signature` 字段填入刚才签名得到的字符串
- `url` 字段的 URL 格式必须是：`https://github.com/用户名/仓库名/releases/download/v版本号/文件名.exe`
- `body` 字段支持 Markdown 格式，会显示在更新对话框中

### 3.5 在 GitHub 上创建 Release

1. **访问你的 GitHub 仓库**
   - 打开 `https://github.com/你的用户名/你的仓库名`

2. **创建新 Release**
   - 点击右侧 "Releases" → "Create a new release"
   - Tag version: 输入 `v2.3.0`（必须以 `v` 开头）
   - Release title: 输入 `版本 2.3.0`
   - Description: 填写更新说明（可以从 CHANGELOG 复制）

3. **上传文件**
   拖拽上传以下两个文件：
   - `PNG部落记账_2.3.0_x64-setup.exe`（安装包）
   - `latest.json`（更新清单）

4. **发布**
   - 点击 "Publish release"

---

## 第四步：测试自动更新

### 4.1 安装旧版本

在测试机器上安装 v2.2.0 版本（当前版本）

### 4.2 检查更新

1. 打开应用
2. 进入 "设置" 页面
3. 点击 "系统更新" 标签
4. 点击 "检查更新" 按钮

### 4.3 预期结果

如果配置正确，应该显示：
- ✅ 发现新版本 v2.3.0
- ✅ 显示发布时间
- ✅ 显示更新内容（从 latest.json 的 body 字段读取）
- ✅ "立即更新" 按钮可点击

### 4.4 执行更新

1. 点击 "立即更新"
2. 确认更新对话框
3. 观察下载进度条
4. 下载完成后，应用会自动重启
5. 重启后，版本应该变为 v2.3.0

---

## 常见问题

### Q1: 点击"检查更新"报错："检查更新失败: ..."

**原因**：
- 公钥配置错误
- GitHub Releases 没有发布
- `latest.json` 文件格式错误
- 网络无法访问 GitHub

**解决**：
1. 检查 `tauri.conf.json` 中的公钥是否正确
2. 确认 GitHub Release 已发布并包含 `latest.json`
3. 在浏览器访问 `https://github.com/你的用户名/你的仓库名/releases/latest/download/latest.json`，看是否能下载
4. 检查 `latest.json` 格式是否正确（使用 JSON 校验工具）

### Q2: 提示"没有可用更新"

**原因**：
- `latest.json` 中的版本号与当前版本相同或更低
- 版本号格式不正确

**解决**：
1. 确认 `latest.json` 中的 `version` 字段是 `"2.3.0"`（字符串，不要 `v` 前缀）
2. 确认当前应用版本确实是旧版本

### Q3: 下载后安装失败

**原因**：
- 签名验证失败（签名字符串错误或私钥不匹配）
- 下载的文件损坏

**解决**：
1. 重新签名安装包，确保使用正确的私钥
2. 确认 `latest.json` 中的签名字符串完整复制
3. 重新上传安装包到 GitHub Releases

### Q4: macOS 上如何打包 Windows 应用？

**答**：Tauri 不支持跨平台打包。必须在 Windows 上打包 Windows 版本，在 macOS 上打包 macOS 版本。

如果需要发布 macOS 版本：
1. 在 macOS 上运行 `npm run tauri:build`
2. 打包产物在 `src-tauri/target/release/bundle/dmg/` 或 `app/`
3. 签名并上传到 GitHub Releases
4. 更新 `latest.json`，添加 `darwin-x86_64` 或 `darwin-aarch64` 平台配置

### Q5: 如何支持多平台更新？

**答**：在 `latest.json` 中添加多个平台配置：

```json
{
  "version": "2.3.0",
  "date": "2026-01-18",
  "platforms": {
    "windows-x86_64": {
      "signature": "Windows 安装包签名",
      "url": "https://github.com/.../PNG部落记账_2.3.0_x64-setup.exe"
    },
    "darwin-x86_64": {
      "signature": "macOS Intel 签名",
      "url": "https://github.com/.../baimo-desktop_2.3.0_x64.dmg"
    },
    "darwin-aarch64": {
      "signature": "macOS Apple Silicon 签名",
      "url": "https://github.com/.../baimo-desktop_2.3.0_aarch64.dmg"
    }
  },
  "body": "更新内容..."
}
```

---

## 📝 发布清单（每次发布时使用）

- [ ] 更新版本号（5 个文件）
- [ ] 更新 CHANGELOG.md
- [ ] 提交并推送代码
- [ ] 在 Windows 上打包应用
- [ ] 在 macOS 上对安装包签名
- [ ] 创建 latest.json 文件
- [ ] 在 GitHub 创建新 Release
- [ ] 上传安装包和 latest.json
- [ ] 发布 Release
- [ ] 测试自动更新功能

---

## 🔒 安全提示

1. **私钥安全**
   - ⚠️ 永远不要将 `~/.tauri/baimo-desktop.key` 提交到 Git
   - ⚠️ 定期备份私钥到安全位置
   - ⚠️ 如果私钥泄露，必须重新生成密钥对并重新发布所有版本

2. **GitHub Token**
   - 如果使用 GitHub Actions 自动发布，需要配置 `GITHUB_TOKEN`
   - Token 应该只有 Release 权限

3. **签名验证**
   - 用户应用会验证安装包签名，确保不被篡改
   - 签名不匹配会导致更新失败，这是安全特性

---

## 📚 参考资料

- [Tauri 自动更新文档](https://tauri.app/v1/guides/distribution/updater)
- [GitHub Releases 文档](https://docs.github.com/en/repositories/releasing-projects-on-github)
- [Tauri CLI 文档](https://tauri.app/v1/api/cli)

---

**文档版本**: 1.0
**最后更新**: 2026-01-17
**适用版本**: Tauri 2.0, 白墨记账系统 v2.2.0+
