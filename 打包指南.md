# PNG部落记账系统 v2.2.0 打包指南

## 📋 前置要求

### 1. 必需软件
- **Node.js** (16+ 或 18+)
  - 检查: `node --version`
  - 下载: https://nodejs.org/

- **Rust** (1.70+)
  - 检查: `cargo --version`
  - 下载: https://rustup.rs/
  - 安装命令: `rustup toolchain install stable`

- **Windows Build Tools** (Windows 10 SDK)
  - Visual Studio Build Tools 2019 或更高版本
  - 或安装 Visual Studio 2022 Community Edition

### 2. 环境变量
确保以下目录在 PATH 中:
- `C:\Users\<用户名>\.cargo\bin` (Rust)
- `C:\Program Files\nodejs` (Node.js)

## 🚀 快速打包

### 方法 1: 使用批处理脚本 (推荐)

1. 双击运行 `build.bat`
2. 等待构建完成（约 5-10 分钟）
3. 查看 `src-tauri\target\release\bundle\nsis\` 目录

### 方法 2: 手动命令

```powershell
# 1. 进入项目目录
cd E:\Users\Administrator\Desktop\baimo_desktop

# 2. 构建前端
cd frontend
npm run build
cd ..

# 3. 构建 Tauri 应用
npm run tauri:build
```

### 方法 3: 分步构建

```powershell
# 步骤 1: 清理旧构建（可选）
Remove-Item -Recurse -Force .\src-tauri\target\release -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .\frontend\dist -ErrorAction SilentlyContinue

# 步骤 2: 构建前端
cd frontend
npm run build

# 步骤 3: 检查前端构建产物
ls dist

# 步骤 4: 构建 Rust 后端和打包
cd ..\src-tauri
cargo build --release

# 步骤 5: 创建安装包
cd ..
cd frontend
npm run tauri build
```

## 📦 构建产物

### 成功后的文件位置

```
src-tauri/target/release/bundle/nsis/
├── PNG部落记账_2.2.0_x64-setup.exe       # 主安装程序 (~120MB)
├── PNG部落记账_2.2.0_x64-setup.exe.zip   # 安装程序压缩包
└── PNG部落记账_2.2.0_x64.nsis.zip        # 便携版 (可选)
```

### 文件说明

| 文件 | 大小 | 用途 |
|------|------|------|
| `*-setup.exe` | ~120MB | 完整安装程序，包含 WebView2 Bootstrapper |
| `*.nsis.zip` | ~80MB | 免安装便携版（需要系统已安装 WebView2） |

## 🔍 故障排除

### 问题 1: 找不到 cargo
```
Error: 'cargo' is not recognized as an internal or external command
```

**解决方案:**
1. 安装 Rust: https://rustup.rs/
2. 重启终端/PowerShell
3. 验证: `cargo --version`

### 问题 2: 前端构建失败
```
Error: Cannot find module ...
```

**解决方案:**
```powershell
cd frontend
npm install
npm run build
```

### 问题 3: Rust 编译错误
```
Error: linker 'link.exe' not found
```

**解决方案:**
安装 Visual Studio Build Tools:
1. 下载: https://visualstudio.microsoft.com/downloads/
2. 选择 "Desktop development with C++"
3. 重启系统

### 问题 4: 内存不足
```
Error: FATAL ERROR: Reached heap limit
```

**解决方案:**
增加 Node.js 内存限制:
```powershell
set NODE_OPTIONS=--max-old-space-size=4096
npm run tauri:build
```

### 问题 5: ImageMagick 未打包
**检查:**
```powershell
ls src-tauri\resources\imagemagick\magick.exe
```

**解决方案:**
确保 `tauri.conf.json` 中的 resources 配置正确:
```json
"resources": [
  "resources/imagemagick/*",
  "resources/imagemagick/modules/**/*"
]
```

## 📊 构建时间参考

| 步骤 | 预计时间 |
|------|----------|
| 前端构建 (npm run build) | 1-2 分钟 |
| Rust 编译 (cargo build --release) | 3-5 分钟 |
| NSIS 打包 | 1-2 分钟 |
| **总计** | **5-10 分钟** |

## ✅ 验证打包结果

### 1. 检查文件大小
```powershell
Get-ChildItem "src-tauri\target\release\bundle\nsis\*.exe" | Format-Table Name, Length
```

预期: 100-150 MB

### 2. 检查版本号
右键点击 `.exe` 文件 → 属性 → 详细信息
- 产品版本: 2.2.0
- 文件版本: 2.2.0

### 3. 测试安装
1. 在虚拟机或测试机器上运行安装程序
2. 检查应用是否正常启动
3. 验证新功能是否正常工作

## 🎯 发布前检查清单

- [ ] 版本号已更新 (2.2.0)
- [ ] CHANGELOG.md 已更新
- [ ] 构建无错误和警告
- [ ] 安装包文件大小正常
- [ ] ImageMagick 已打包
- [ ] 测试机器安装验证通过
- [ ] 所有新功能测试通过

## 📝 打包日志

如需保存详细日志:
```powershell
npm run tauri:build 2>&1 | Tee-Object -FilePath "build.log"
```

查看日志:
```powershell
Get-Content "build.log" -Tail 50
```

## 💡 优化建议

### 减少构建时间
1. 使用 SSD 硬盘
2. 关闭杀毒软件实时扫描
3. 增加系统内存分配

### 减少安装包大小
1. 启用代码压缩
2. 移除未使用的依赖
3. 使用 WebView2 运行时链接而非嵌入

## 🔗 相关资源

- Tauri 文档: https://tauri.app/
- Rust 文档: https://www.rust-lang.org/
- NSIS 文档: https://nsis.sourceforge.io/

---

**最后更新**: 2026-01-16
**适用版本**: v2.2.0
